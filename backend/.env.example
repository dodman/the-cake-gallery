NODE_ENV=development
PORT=5000
DATABASE_URL=postgresql://USER:PASS@ep-xxx.neon.tech/neondb?sslmode=require
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=7d
# Admin password used when seeding the database (npm run seed). Choose a strong value; never commit the real one.
ADMIN_PASSWORD=
# Used only by the password-rotation script (npx tsx src/setAdminPassword.ts) to change the live admin password.
NEW_ADMIN_PASSWORD=
# Allowed website origin(s) for CORS — comma-separated, no trailing slash.
# Production example: https://thecakegallery.online,https://www.thecakegallery.online
CORS_ORIGIN=http://localhost:3000
EMAIL_FROM=waka.bk29@gmail.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMS_PROVIDER=mock
